## 1. 架构设计
```mermaid
graph TB
    subgraph "前端层（React 18 + Vite）"
        A["触屏终端 UI 层"] --> B["路由与导航（React Router）"]
        B --> C["7 大页面组件"]
        C --> D["全局状态管理（Zustand）"]
        D --> E["Mock 数据服务层"]
    end
    subgraph "样式与交互"
        F["TailwindCSS 3 + 自定义主题"] --> G["动画与过渡（Framer Motion）"]
        H["图标库（Lucide React）"] --> C
    end
    subgraph "数据层"
        I["本地 Mock 数据（JSON/TS）"] --> E
        J["LocalStorage 持久化"] --> D
    end
```

## 2. 技术说明
- **前端框架**：React@18 + TypeScript@5 + Vite@5
- **初始化工具**：pnpm create vite（React + TypeScript 模板）
- **路由**：react-router-dom@6
- **状态管理**：zustand@4（轻量、极简 API，适合终端场景）
- **样式方案**：tailwindcss@3 + postcss + autoprefixer
- **动画库**：framer-motion@11（页面切换、微交互）
- **图标库**：lucide-react@0.400
- **二维码生成**：qrcode.react@3
- **图表**：recharts@2（统计看板柱状图、饼图）
- **后端**：无，纯前端 Mock 数据模拟
- **数据库**：无，使用 TypeScript 常量 + localStorage 模拟持久化

## 3. 路由定义
| 路由 | 用途 |
|-----|------|
| / | 重定向到场地日历页 |
| /calendar | 场地日历页（首页） |
| /login | 会员识别页 |
| /booking | 预约下单页 |
| /entry | 入场核验页 |
| /reschedule | 临时改签页 |
| /courses | 课程报名页 |
| /feedback | 公告与反馈页 |

## 4. 数据模型
### 4.1 数据模型定义
```mermaid
erDiagram
    MEMBER ||--o{ BOOKING : has
    MEMBER ||--o{ COUPON : owns
    VENUE ||--o{ COURT : contains
    COURT ||--o{ TIME_SLOT : has
    BOOKING }o--|| COURT : reserves
    BOOKING }o--|| TIME_SLOT : uses
    BOOKING }o--|| COUPON : uses
    COURSE }o--|| COACH : taught_by
    COURSE ||--o{ ENROLLMENT : has
    MEMBER ||--o{ ENROLLMENT : joins
    ANNOUNCEMENT ||--o{ VENUE : targets
    FEEDBACK }o--|| MEMBER : submits

    MEMBER {
        string id PK
        string name
        string memberNo
        string level
        number balance
        string avatar
        string phone
    }
    VENUE {
        string id PK
        string name
        string type
        string icon
        string status
    }
    COURT {
        string id PK
        string venueId FK
        string name
        number pricePerHour
        string facilities
    }
    TIME_SLOT {
        string id PK
        string courtId FK
        string date
        string startTime
        string endTime
        string status
        number capacity
    }
    BOOKING {
        string id PK
        string memberId FK
        string courtId FK
        string slotId FK
        string status
        number peopleCount
        number totalAmount
        number discount
        number payAmount
        string payStatus
        string entryCode
        datetime createdAt
    }
    COUPON {
        string id PK
        string memberId FK
        string name
        number discount
        string type
        datetime expireAt
        boolean used
    }
    COACH {
        string id PK
        string name
        string avatar
        string specialty
        number experience
        number rating
    }
    COURSE {
        string id PK
        string name
        string coachId FK
        string venueType
        datetime startTime
        number duration
        int capacity
        int enrolled
        number price
    }
    ENROLLMENT {
        string id PK
        string memberId FK
        string courseId FK
        string status
        datetime createdAt
    }
    ANNOUNCEMENT {
        string id PK
        string title
        string content
        string type
        string[] targetVenues
        datetime publishAt
        datetime expireAt
    }
    FEEDBACK {
        string id PK
        string memberId FK
        string type
        int rating
        string content
        datetime createdAt
    }
```

### 4.2 目录结构
```
src/
├── assets/              # 静态资源（图片、字体）
├── components/          # 通用组件
│   ├── layout/          # 布局组件（侧边栏、顶栏）
│   ├── ui/              # 基础UI（按钮、卡片、输入框）
│   └── features/        # 业务组件（日历、时段图等）
├── pages/               # 7大页面
│   ├── Calendar.tsx
│   ├── Login.tsx
│   ├── Booking.tsx
│   ├── Entry.tsx
│   ├── Reschedule.tsx
│   ├── Courses.tsx
│   └── Feedback.tsx
├── store/               # Zustand stores
│   ├── useMemberStore.ts
│   ├── useBookingStore.ts
│   └── useUiStore.ts
├── data/                # Mock 数据
│   ├── venues.ts
│   ├── members.ts
│   ├── bookings.ts
│   ├── courses.ts
│   └── announcements.ts
├── types/               # TypeScript 类型定义
│   └── index.ts
├── utils/               # 工具函数
│   ├── date.ts
│   ├── format.ts
│   └── qrcode.ts
├── hooks/               # 自定义 hooks
├── App.tsx
├── main.tsx
└── index.css
```

## 5. 全局状态设计
- **useMemberStore**：当前登录会员信息、登录状态、登出方法
- **useBookingStore**：选中的场馆/日期/时段/场地、购物车订单、优惠券、入场码
- **useUiStore**：侧边栏折叠状态、全局 Loading、Toast 消息栈、当前激活 Tab

## 6. 触屏交互规范
- 所有按钮 `min-width: 72px, min-height: 72px`（主操作）
- 次要点击目标 `min-width: 48px, min-height: 48px`
- 防止误触：`touch-action: manipulation`
- 列表惯性滚动：`-webkit-overflow-scrolling: touch`
- 长按反馈：0.5s 触发二级菜单/详情
- 滑动手势：水平切换日期、垂直滚动列表
